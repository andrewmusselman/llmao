# Hayward gateway — Phase 1 runtime dependencies.
quart>=0.19
# Production extras (not needed for dev/mock mode):
requests>=2.31        # ProxyBackend -> litellm proxy admin + chat calls
# asfquart>=0.1        # provides ASF OAuth + LDAP + PAT in HAYWARD_AUTH_MODE=asf
#                        install from https://github.com/apache/infrastructure-asfquart
